<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
<mapper namespace="web.dao.SemiDAO">

  <select id="loginById"  resultType="semiVO"   parameterType="semiVO" >
	<![CDATA[
		select * from member	
		where id=#{id} and pw=#{pw}		
	]]>
  </select>
	
</mapper>
